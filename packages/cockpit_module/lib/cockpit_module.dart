/// Module system for the Cockpit super-app: the [CockpitModule] contract and
/// the [ModuleRegistry] that aggregates active modules for the shell.
library;

export 'src/cockpit_module.dart';
export 'src/module_registry.dart';
