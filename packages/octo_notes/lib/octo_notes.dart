/// OctoNotes — the lecture-capture & AI notes module for the Octopilot
/// super-app.
///
/// The shell only needs [OctoNotesModule]; everything else is internal.
library;

export 'src/octo_notes_module.dart';
